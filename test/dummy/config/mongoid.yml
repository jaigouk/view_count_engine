development:
  sessions:
    default:
      database: dummy_dev
      hosts:
        - localhost:27017
      options:
        max_retries: 1
        retry_interval: 0

test:
  sessions:
    default:
      database: dummy_test
      hosts:
        - localhost:27017
      options:
        max_retries: 1
        retry_interval: 0
